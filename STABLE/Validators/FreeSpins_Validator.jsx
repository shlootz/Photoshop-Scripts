if (typeof FREESPINS_VALIDATOR !== "object") {    FREESPINS_VALIDATOR = {};}(function () {        if (typeof FREESPINS_VALIDATOR.validate !== "function") {        meta = {    // table of character substitutions            "\b": "\\b",            "\t": "\\t",            "\n": "\\n",            "\f": "\\f",            "\r": "\\r",            "\"": "\\\"",            "\\": "\\\\"        };        FREESPINS_VALIDATOR.validate = function (mode, json, emptyPanels){        switch (mode){               case "desktop":                    FREESPINS_VALIDATOR.DESKTOP_MODEL.populate();                    return validate(json, FREESPINS_VALIDATOR.DESKTOP_MODEL, emptyPanels);                    break;                               case "mobile_portrait":                    FREESPINS_VALIDATOR.PORTRAIT_MODEL.populate();                    return validate(json, FREESPINS_VALIDATOR.PORTRAIT_MODEL, emptyPanels);                    break;                                case "mobile_landscape":                    FREESPINS_VALIDATOR.LANDSCAPE_MODEL.populate();                     return validate(json, FREESPINS_VALIDATOR.LANDSCAPE_MODEL, emptyPanels);                    break;                                default:                    confirm ("File name missing 'desktop', 'mobile_portrait' or 'mobile_landscape' Continue?", true, "No validator for this file.");                    if(!result) {                                 return "canceled";                             }                    break;            }        }    }    function validate(json, model, emptyPanels){                    var passes = [true];            var slots = json["slots"];                        for(var key in slots){                   var depth = slots[key]["layer"].replace(/[^\/]/g, "").length;                   var tree = slots[key]["layer"].split("/");                   var root = tree[0];                                       slots[key]["layer"] =  slots[key]["layer"].toLowerCase();                }                        for(var i = 0; i<model.layers.length; i++){                var found = false;                 for(var key in slots){                           if(slots[key]["layer"].indexOf(model.layers[i].toLowerCase()) != -1){                              found = true;                          }                       else {                           var path = (model.layers[i]).split("/");                            var parent = (path[path.length-3]);                           var child = (path[path.length-2]);                                                      for(val in emptyPanels){                                var emptyChild = (stripJunkFromName(val)[0]);                                var emptyParent = (stripJunkFromName(val)[1]);                                                                     if(child === emptyChild && emptyParent.indexOf(parent) != -1) {                                            found = true;                                         }                                                                }                           }                       }                 if(!found){                    var result = confirm ("You are missing the "+model.layers[i]+" group from your file or it's empty! Continue?", true, "Missing or Empty Photoshop Groups");                             if(!result) {                                 return "canceled";                             }                    }                 passes.push(found);                 }                        return evaluate(passes);        }            function stripJunkFromName(name){        var str = name;        str = str.replace(".png", "");        return ([str.split("!@")[0],str.split("!@")[1]]);        }        function evaluate(passes){        var result = true;        for (var i = 0; i<passes.length; i++){            if(passes[i] === false){                    result = false;                }            }                return result;        }   })();//////////////// MODELS //////////////////////////////// DESKTOP ///////////////FREESPINS_VALIDATOR.DESKTOP_MODEL = {};FREESPINS_VALIDATOR.DESKTOP_MODEL.populate = function(){        var layers=[];    //fail test    layers.push("SymbolsOverground/FreeSpins/");    layers.push("Logo/FreeSpins/");    layers.push("freeSpinsPopupExtra/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupExtra/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupExtra/BackgroundContainer/");    layers.push("freeSpinsPopupEnd/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupEnd/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupEnd/BackgroundContainer/");    layers.push("freeSpinsPopupStart/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupStart/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupStart/BackgroundContainer/");    layers.push("BackgroundModuleAssets/FreeSpins/");        layers.push("WideBackground/FreeSpins/");        FREESPINS_VALIDATOR.DESKTOP_MODEL.layers = layers;    }//////////////// LANDSCAPE MOBILE ///////////////FREESPINS_VALIDATOR.LANDSCAPE_MODEL = {};FREESPINS_VALIDATOR.LANDSCAPE_MODEL.populate = function(){        var layers=[];    //fail test    layers.push("SymbolsOverground/Main_landscape/FreeSpins/");    layers.push("Logo/Main_landscape/FreeSpins/");    layers.push("freeSpinsPopupExtra/Main_landscape/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupExtra/Main_landscape/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupExtra/Main_landscape/BackgroundContainer/");    layers.push("freeSpinsPopupEnd/Main_landscape/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupEnd/Main_landscape/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupEnd/Main_landscape/BackgroundContainer/");    layers.push("freeSpinsPopupStart/Main_landscape/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupStart/Main_landscape/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupStart/Main_landscape/BackgroundContainer/");    layers.push("BackgroundModuleAssets/Main_landscape/FreeSpins/");        layers.push("WideBackground/Main_landscape/FreeSpins/");        FREESPINS_VALIDATOR.LANDSCAPE_MODEL.layers = layers;    }//////////////// PORTRAIT MOBILE ///////////////FREESPINS_VALIDATOR.PORTRAIT_MODEL = {};FREESPINS_VALIDATOR.PORTRAIT_MODEL.populate = function(){        var layers=[];    //fail test    layers.push("SymbolsOverground/Main_portrait/FreeSpins/");    layers.push("Logo/Main_portrait/FreeSpins/");    layers.push("freeSpinsPopupExtra/Main_portrait/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupExtra/Main_portrait/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupExtra/Main_portrait/BackgroundContainer/");    layers.push("freeSpinsPopupEnd/Main_portrait/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupEnd/Main_portrait/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupEnd/Main_portrait/BackgroundContainer/");    layers.push("freeSpinsPopupStart/Main_portrait/FreeSpinsPopupTexts/");    layers.push("freeSpinsPopupStart/Main_portrait/freeSpinsPopupGraphics/");    layers.push("freeSpinsPopupStart/Main_portrait/BackgroundContainer/");    layers.push("BackgroundModuleAssets/Main_portrait/FreeSpins/");        layers.push("WideBackground/Main_portrait/FreeSpins/");        FREESPINS_VALIDATOR.PORTRAIT_MODEL.layers = layers;    }