#target photoshopapp.bringToFront();var originalDoc;try {	originalDoc = app.activeDocument;} catch (ignored) {$.writeln("1 ERROR "+ignored)}var nameCounter = 0;var allLayers = [];returnAllLayers(originalDoc, allLayers);function returnAllLayers (doc, allLayers){            var curr = 0;            for (var m = 0; m < doc.layers.length; m++){                var theLayer = doc.layers[m];                if(theLayer.visible){                    nameCounter ++;                        curr ++;                            theLayer.name = stripJunkFromName(theLayer.name)                           }                    if (theLayer.layers && theLayer.layers.length > 0)                    {                         returnAllLayers(theLayer, allLayers);                    } else {                }            }            return allLayers;}function stripJunkFromName(name){var str = name;str = str.replace(".png", "");return (str.split("!@")[0]);}